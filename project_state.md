# حالة المشروع (Project State)

## نبذة عامة
نظام ويب محلي خفيف (PWA) لتوثيق ومتابعة تقارير حصص القرآن الكريم اليومية لمعلم القرآن محمد نبيل، مبني بجافاسكربت الصرفة (Vanilla JS) ومحلي بالكامل (Local-First) دون أي اعتماديات خارجية ثقيلة.

## القرارات المعمارية والتغييرات الأخيرة
- **إلغاء حقلي الفترة والفئة:**
  - أُسقِط حقلا "الفترة" (`halaType`) و"الفئة" (`studentCategory`) من واجهة التطبيق وشريط الملخص وقاعدة بيانات المسودات `drafts` والحفظ المحلي `localStorage`، وحُذِفت الإشارة إليهما من نصوص التقرير المنسوخ وجدول تصدير مستند PDF، لتصبح بيانات الجلسة مقتصرة على: التاريخ، المعلم (محمد نبيل)، المدة، ورقم الحلقة (اختياري).
- **إعادة تسمية المنظومة (Rebranding):**
  - استُبدِل مسمى "أكاديمية سنا" في كافة أرجاء النظام ليصبح "تقرير حصة القرآن الكريم"، شاملاً عنوان الصفحة والترويسة، وملف البيان `manifest.json`، وترويسة التقارير النصية المشتركة، وقالب تصدير PDF واسم الملف الصادر، والشعار البصري `assets/icon.svg`.
- **إلغاء آلية الإرسال للاستمارة (Google Forms):**
  - حُذِف زر "إرسال للاستمارة" من شريط الإجراءات ودالته `submitToGoogleForm` من `report.js` وثوابت `FORM_CONFIG` من `constants.js`، ليقتصر التطبيق على نسخ التقرير نصياً وتصدير وثيقة PDF.
- **تثبيت المعلم وإلغاء آلية الاختيار:**
  - أُلغيت قائمة الاختيار المتعدد للمعلمين من الواجهة، وثُبِّت اسم المعلم صراحة على `محمد نبيل` عبر الثابت `TEACHER_NAME` في `constants.js`.
  - اعتُمِد الاسم الثابت في شريط ملخص بيانات الحلقة، ونصوص التقارير المنشورة، وتصدير PDF، والحفظ التلقائي في `localStorage`.
- **إلغاء آلية الاختيار من المصحف (Quran Picker):**
  - حُذِفت نافذة منتقي المصحف المنبثقة من الواجهة، وأزيلت أزرار المصحف الفرعية في بطاقات الطلاب لتبسيط عملية الرصد وتخفيف حجم التطبيق.
  - أُزيلت ملفات الفهرس الضخمة (`quran_surahs.json`, `quran_juzs.json`, `quran-data.js`, `quran.js`).
  - رُقِّيَ كاش عامل الخدمة إلى `mohammed-report-v2` ونُظِّفت قائمة الأصول المخبأة لمنع أي فشل في التثبيت دون اتصال.
- **تدقيق الشفرات النظيفة (Clean Code Guard):**
  - استُخرجت الدالة المشتركة `syncCurrentDraft` لإلغاء تكرار كود مزامنة المسودة الحالية عبر `drafts.js` و `settings.js`.
  - أُزيلت ميثود `String.prototype.substr` المهجورة واستُبدلت بـ `slice(2, 6)` في توليد معرفات المسودات.
  - وحِّد الاعتماد على الثابت المباشر `TEACHER_NAME` وأُزيل البحث الفائض عن عنصر المعلم في DOM مع حذف الحقل الخفي من `index.html`.
  - استُخرجت الدالة المساعدة `formatHoursText` لإلغاء تكرار شروط صيغ الجموع العربية في بناء نصوص المدة الزمنية.
  - رُقِّيَ كاش عامل الخدمة إلى `mohammed-report-v3` مع تسجيل إخفاقات التصدير المباشر في سجلات وحدة التحكم.
- **الحقول المدعومة في بطاقة الطالب:**
  - اسم الطالب، الجنس، التسميع وتقييمه، الماضي وتقييمه، الماضي القريب وتقييمه، الحفظ الجديد، الماضي المطلوب، الماضي القريب المطلوب، ملاحظات المعلم، والوسام التكريمي.
- **النشر والاستضافة (Deployment):**
  - المستودع البعيد: [kamalyaser31/mohammed-report](https://github.com/kamalyaser31/mohammed-report)
  - رابط GitHub Pages المباشر: [https://kamalyaser31.github.io/mohammed-report/](https://kamalyaser31.github.io/mohammed-report/)
  - الفرع المعتمد للبناء: `main` (Root path `/`).


